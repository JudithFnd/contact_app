# contact_app

--------------------------------------------------------------------------
npm install

After cloning the repository, run npm install to install the project dependencies.

--------------------------------------------------------------------------

To run the application locally, execute ng serve and open http://localhost:4200 (or the port you take) in your browser.

--------------------------------------------------------------------------